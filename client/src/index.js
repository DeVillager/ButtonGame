// Import modules.
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// Renders the React-component from App-class into the html div component, which has id='root'. 
ReactDOM.render(<App />, document.getElementById('root'));

